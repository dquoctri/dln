import React, { Fragment } from 'react'
import { Link } from 'react-router-dom'
import AppTitle from '../../components/app-title'

const Dashboard = () => {
  return (
    <Fragment>
      <AppTitle title='Dash-Deadline' />
      <div>Dashboard</div>
    </Fragment>
  )
}

export default Dashboard
